[project]
name = "foobar"
version = "0.1.0"
requires-python = ">=3.8,<4.0"
dependencies = [
    "docker>=4.3.1",
]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
