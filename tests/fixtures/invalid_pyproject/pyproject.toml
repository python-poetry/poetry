[tool.poetry]
name = "invalid"
version = "1.0.0"
authors = [
    "Foo <foo@bar.com>"
]
license = "INVALID"

[tool.poetry.dependencies]
python = "*"
pendulum = {"version" = "^2.0.5", allows-prereleases = true}
